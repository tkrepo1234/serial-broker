{
  "name": "serial-broker-terminal-example",
  "version": "0.0.0",
  "private": true,
  "description": "A serial terminal built on serial-broker: static HTML, an import map, no build step.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node serve.mjs",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "serial-broker": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^6.0.3"
  }
}
