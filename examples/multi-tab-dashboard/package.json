{
  "name": "serial-broker-multi-tab-dashboard-example",
  "version": "0.0.0",
  "private": true,
  "description": "Several tabs on one device: a plain-DOM dashboard for serial-broker, with Vite and TypeScript.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "vite --port 8152 --strictPort",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "preview": "vite preview --port 8152 --strictPort"
  },
  "dependencies": {
    "serial-broker": "file:../.."
  },
  "devDependencies": {
    "@types/w3c-web-serial": "^1.0.8",
    "typescript": "^6.0.3",
    "vite": "^8.3.0"
  }
}
