{
  "name": "serial-broker-minimal-example",
  "version": "0.0.0",
  "private": true,
  "description": "The smallest page that uses serial-broker: connect, print what arrives, send text.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "vite --port 8151 --strictPort",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "preview": "vite preview --port 8151 --strictPort"
  },
  "dependencies": {
    "serial-broker": "file:../.."
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.3.0"
  }
}
