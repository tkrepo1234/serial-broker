# Diagnostics

:::{admonition} Not written yet
:class: note

How to see what serial-broker is doing in a running deployment: the opt-in logger, the
`serial-broker/diagnostics` entry point that reports every tab's role, connection and pending writes,
and the debugging surface shipped in `dist/debug/` — how to serve it, point it at the application's
worker, and whether to expose it at all.
:::
