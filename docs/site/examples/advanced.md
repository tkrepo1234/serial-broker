# Advanced

:::{admonition} Not written yet
:class: note

The hard cases: commands that must survive failover, including deciding what to do after
`OWNER_LOST_DURING_WRITE`; a line- or frame-based protocol layer with request–response correlation on
top of serial-broker; command sequences that must not be interleaved with other tabs; and several
devices driven from the same tabs.
:::
