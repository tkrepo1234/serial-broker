# Internals

:::{admonition} Not written yet
:class: note

How serial-broker is built, for people changing it: the layers, the message protocol between tabs
and its versioning, the owner's connection state machine, and how the test suite simulates several
tabs and devices. The architectural decisions behind each part are recorded as ADRs in the
repository's `docs/adr/` directory.
:::
