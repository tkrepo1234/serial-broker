# Configuration

:::{admonition} Not written yet
:class: note

Every option in depth — what it does, its default, its range, what changing it costs, and when you
would — for `setup()` (`device`, `serial`, `connection`, `encoding`, `persist`) and for
`configure()` (`workerUrl`, `transport`, `logger`, `logPayloads`). Until then, the defaults and
ranges are listed in the [API reference](api/index.md), and
[How shared ports behave](shared-ports.md) explains the connection settings in context.
:::
