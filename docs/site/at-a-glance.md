# At a glance

What serial-broker does, where it fits, and which problem each part of it solves — on one page.

```{image} _static/serial-broker-at-a-glance.svg
:alt: serial-broker at a glance: every tab uses one serial port, another tab takes over when the holding tab closes, the connection returns with the device, and each feature is matched to the problem it solves.
:width: 100%
:target: _static/serial-broker-at-a-glance.svg
```

Every statement in the illustration is explained in the chapters: start with the
[Introduction](introduction.md) or go straight to the [Quickstart](quickstart.md).
