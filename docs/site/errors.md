# Errors

:::{admonition} Not written yet
:class: note

How errors are reported — as rejected promises and through `onError` — and every error code with
the situations that produce it, what it means for the application, and what to do about it.
Until then, every error carries its own `remediation` sentence, and the codes are listed in the
[API reference](api/index.md).
:::
