# Dependencies
node_modules/

# Build output
dist/
coverage/
*.tsbuildinfo

# Editor / OS
.DS_Store
Thumbs.db
.idea/

# Browser tests: Playwright's traces, reports and the throwaway browser profiles under them
# (ADR-0021)
test-results/
playwright-report/

# Logs
*.log
npm-debug.log*

# Documentation site: Python environment, generated reference, build output (ADR-0016)
docs/.venv/
docs/site/api/reference/
docs/site/_build/

# The benchmark's bundled runner (bench/run.mjs); its results under bench/results/ are committed
bench/.build/

# Agent worktrees and local Claude Code state
.claude/

# Example applications: their dependencies, builds and caches
examples/*/node_modules/
examples/*/dist/
